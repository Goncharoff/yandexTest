<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ignition.yandextest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PicturesList.PictureListActivity"
            android:screenOrientation="portrait"
            android:label="@string/picture_list_eng"/>
        <activity android:name=".FullImageSizeScreen.FullImageScreenActivity" />
        <activity android:name=".ImageInfoActivity.ImageInfoActivity"
            android:label="@string/image_info_eng"></activity>
    </application>

</manifest>